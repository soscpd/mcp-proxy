apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mcpeto.fullname" . }}
  labels:
    {{- include "mcpeto.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Values.config | toPrettyJson | nindent 4 }}
