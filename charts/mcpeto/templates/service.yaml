apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcpeto.fullname" . }}
  labels:
    {{- include "mcpeto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcpeto.selectorLabels" . | nindent 4 }}
